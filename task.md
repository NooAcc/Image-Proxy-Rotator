# Edge 浏览器扩展开发提示词：漫画网站图片代理分流插件

## 角色
你是一名资深浏览器扩展开发工程师，精通 Edge / Chrome Manifest V3、Service Worker、网络请求拦截与代理实现。

## 背景
漫画网站在阅读时会预加载全部图片。网站存在单 IP 速率限制，导致部分图片加载失败。需要通过浏览器插件对指定网络请求进行代理分流，以突破单 IP 限制。

## 核心目标
开发一款 **Edge 浏览器扩展**（兼容 Chromium），自动对匹配的网络请求使用代理列表进行**轮询分流**，解决图片加载失败问题。

---

## 功能需求

### 1. 设置页面（Options Page）
- 可配置**代理节点列表**
- 可配置需要代理的**网络请求 URL 规则**
- 支持增删改、导入/导出配置
- 配置持久化保存（使用 `chrome.storage`）

### 2. 代理节点支持
- **基础协议**：HTTP、HTTPS、SOCKS5
- **高级协议**（sing-box 支持的格式）：VLESS、Hysteria2 等
- 节点格式示例需兼容常见订阅/节点链接（如 `http://`、`socks5://`、`vless://`、`hysteria2://` 等）
- 代理逻辑**完全由浏览器扩展自身实现**，优先使用纯 JavaScript / Web API / Service Worker 能力（不依赖外部本地客户端，如不强制依赖系统代理或本地 sing-box 进程）
- 若高级协议在纯 JS 环境下无法完整实现，需明确说明限制，并给出可行的折中方案（例如仅支持基础协议，或提示用户配合本地代理）

### 3. 代理分流与轮询
- 对匹配的网络请求自动使用代理列表进行**轮询（Round-Robin）分流**
- 支持手动启用/禁用单个节点
- **延迟测试功能**：可一键或定时测试所有节点延迟
- **自动禁用**：无法连接或超时的节点自动标记为不可用，并在轮询中跳过
- 支持手动重新启用被禁用的节点

### 4. URL 匹配规则
- 支持**精确 URL** 匹配
- 支持**正则表达式**匹配
- 规则可单独启用/禁用
- 仅对匹配规则的请求走代理，其他请求保持正常

### 5. 状态页面（Popup 或独立页面）
- 实时显示当前启用的代理节点数量、可用/不可用状态
- 显示最近请求的代理使用情况（节点、延迟、成功/失败）
- 快速开关插件总开关
- 一键测试全部节点延迟
- 查看当前生效的 URL 规则

---

## 技术约束与建议
- 使用 **Manifest V3**
- 优先使用 `declarativeNetRequest` + `chrome.proxy`（如适用）或 Service Worker 拦截（`webRequest` / `fetch` 拦截）实现请求转发
- 纯 JS 实现代理时，注意 CORS、HTTPS 证书、WebSocket 等限制，需在文档中说明
- 界面简洁、响应式，支持中文
- 代码结构清晰，便于维护（设置页、状态页、后台逻辑分离）
- 做好错误处理与日志（可在状态页查看）

---

## 交付要求
请输出完整可运行的扩展代码结构，包括：
1. `manifest.json`
2. 设置页面（HTML + CSS + JS）
3. 状态/弹窗页面
4. 后台 Service Worker 逻辑（代理轮询、延迟测试、请求拦截）
5. 必要的工具函数（节点解析、延迟测试、存储管理等）
6. 使用说明（如何导入节点、配置规则、测试延迟）

如果某些高级协议（VLESS / Hysteria2）在纯浏览器环境中无法完整实现，请明确指出技术限制，并优先保证 HTTP/SOCKS5 的完整可用方案。

---

## 计划文件要求
- **文件名**：`plan.md`（放在输出目录根目录）。
- **必须包含的内容**（使用 Markdown 格式）：
  1. **总体目标与范围**
  2. **当前阶段与进度**（明确标记：未开始 / 进行中 / 已完成）
  3. **详细执行步骤**（可拆分为更细的子步骤）
  4. **已发现的问题与风险**
  5. **已做出的决策与优化记录**
  6. **下一步行动**
  7. **文件统计与进度追踪**（提取了多少文件、多少文本、完成百分比等）
  8. **变更日志**（每次更新时追加时间戳和修改内容）

- **更新规则**（必须严格执行）：
  - 任务开始前：先创建完整初始计划。
  - 每完成一个主要步骤或发现新情况：立即更新计划文件。
  - 遇到障碍、需要调整策略、发现更优方法时：优化计划并记录原因。
  - 每次更新后，在回复中简要说明「计划已更新」及主要变更点。
  - 计划文件本身也要保持清晰、可执行，避免冗余。