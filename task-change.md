【任务目标变更 — 立即生效】

当前项目为漫画图片代理分流扩展（page-proxy）。因进度可能已推进，请以「目标与约束」为准调整后续工作，不要假设某一文件或某一 Task 仍停留在旧状态；先核对现状再改。

## 新目标

1. **完全取消高级协议**  
   不再支持 vless / hysteria2（含 hy2）/ trojan / ss。  
   解析、存储、UI、导出、文档、测试中与上述协议相关的能力一律移除或停用，主流程不得依赖本地网桥或第三方内核。

2. **仅支持 HTTP / HTTPS 代理**  
   可用节点只允许协议为 http 或 https。  
   遇到其他类型（含 socks、上述高级协议及未知协议）时：不得进入可用轮询池，并**自动用简体中文提示**：本程序不支持该代理类型，仅支持 HTTP/HTTPS 代理。

3. **认证**  
   HTTP/HTTPS 代理的账号密码仍按浏览器能力处理（自动应答认证；凭据不得写入 PAC）。  
   非 HTTP/HTTPS 即使带账号密码也不支持。

## 交付边界

**必须保留（若已实现则保持可用，若未完成则按此继续）**  
- HTTP/HTTPS 节点管理、规则、PAC 轮询分流、测速与自动禁用、启停、配置持久化与导入导出、状态与日志、总开关。

**必须不再作为目标**  
- 任何高级协议的可用路由、网桥端口、sing-box（或同类）配置导出、以及「非 HTTP/HTTPS 可正常分流」的验收。

**导入/提示**  
- 混合粘贴时只接纳 HTTP/HTTPS；其余给出明确不支持提示。  
- 历史配置里若仍有非 HTTP/HTTPS 节点，加载后应剔除或禁用，并同样提示，避免静默参与分流。

## 执行方式

1. 对照代码与计划的**当前实际状态**，收敛范围与验收标准，避免按过时任务编号或路径硬改。  
2. 优先保证主路径：仅 HTTP/HTTPS 节点 → 规则 → 测速 → 开启 → 匹配请求轮询分流。  
3. 清理与新目标冲突的逻辑与文案；测试与说明与之一致。  
4. 先用几句话确认你理解的新范围与将改动的方向，再动手修改。